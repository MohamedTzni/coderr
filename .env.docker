# Django
SECRET_KEY=AENDERN-sehr-langen-zufaelligen-string-hier-eintragen
DEBUG=False

# Erlaubte Hosts
ALLOWED_HOSTS=api.mohamed-touzani.de,localhost,127.0.0.1

# CORS
CORS_ALLOWED_ORIGINS=https://coderr.mohamed-touzani.de

# Datenbank (PostgreSQL via Docker — DB_HOST muss "db" bleiben)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=coderr_db
DB_USER=coderr_user
DB_PASSWORD=AENDERN-sicheres-passwort-hier-eintragen
DB_HOST=db
DB_PORT=5432
